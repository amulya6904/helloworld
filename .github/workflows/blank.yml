name: Python Test CI

on:
 workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Python Test
        run: python3 test_calc.py
